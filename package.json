{
  "name": "@erp-next/root",
  "author": {
    "name": "warjamma",
    "email": "5lgold141@gmail.com",
    "url": "https://warjamma.com"
  },
  "private": true,
  "workspaces": [
    "packages/*",
    "tooling/*",
    "apps/*"
  ],
  "scripts": {
    "build": "yarn workspace @erp-next/ui build && yarn workspace @erp-next/utilities build && yarn workspace @erp-next/frappe-ui build",
    "build:debug": "yarn workspace @erp-next/ui build:debug && yarn workspace @erp-next/utilities build:debug && yarn workspace @erp-next/frappe-ui build:debug",
    "dev": "yarn workspaces foreach -p run dev",
    "lint": "yarn workspaces foreach run lint",
    "release": "changeset",
    "version-packages": "changeset version && yarn install && yarn build",
    "publish-packages": "changeset publish"
  },
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/16521524/erp-next.git"
  },
  "devDependencies": {
    "@changesets/cli": "^2",
    "eslint": "^9",
    "eslint-config-next": "^15.4.6",
    "tsup": "^8",
    "typescript": "^5"
  }
}
